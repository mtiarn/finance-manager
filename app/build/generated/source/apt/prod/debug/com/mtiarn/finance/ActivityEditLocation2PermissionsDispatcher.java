// This file was generated by PermissionsDispatcher. Do not modify!
package com.mtiarn.finance;

import android.support.v4.app.ActivityCompat;
import java.lang.Override;
import java.lang.String;
import java.lang.ref.WeakReference;
import permissions.dispatcher.PermissionRequest;
import permissions.dispatcher.PermissionUtils;

final class ActivityEditLocation2PermissionsDispatcher {
  private static final int REQUEST_STARTDETECTCOORDS = 4;

  private static final String[] PERMISSION_STARTDETECTCOORDS = new String[] {"android.permission.ACCESS_FINE_LOCATION","android.permission.ACCESS_COARSE_LOCATION"};

  private ActivityEditLocation2PermissionsDispatcher() {
  }

  static void startDetectCoordsWithPermissionCheck(ActivityEditLocation2 target) {
    if (PermissionUtils.hasSelfPermissions(target, PERMISSION_STARTDETECTCOORDS)) {
      target.startDetectCoords();
    } else {
      if (PermissionUtils.shouldShowRequestPermissionRationale(target, PERMISSION_STARTDETECTCOORDS)) {
        target.showRationaleForContact(new ActivityEditLocation2StartDetectCoordsPermissionRequest(target));
      } else {
        ActivityCompat.requestPermissions(target, PERMISSION_STARTDETECTCOORDS, REQUEST_STARTDETECTCOORDS);
      }
    }
  }

  static void onRequestPermissionsResult(ActivityEditLocation2 target, int requestCode,
      int[] grantResults) {
    switch (requestCode) {
      case REQUEST_STARTDETECTCOORDS:
      if (PermissionUtils.verifyPermissions(grantResults)) {
        target.startDetectCoords();
      } else {
        if (!PermissionUtils.shouldShowRequestPermissionRationale(target, PERMISSION_STARTDETECTCOORDS)) {
          target.onLocationNeverAskAgain();
        } else {
          target.onLocationDenied();
        }
      }
      break;
      default:
      break;
    }
  }

  private static final class ActivityEditLocation2StartDetectCoordsPermissionRequest implements PermissionRequest {
    private final WeakReference<ActivityEditLocation2> weakTarget;

    private ActivityEditLocation2StartDetectCoordsPermissionRequest(ActivityEditLocation2 target) {
      this.weakTarget = new WeakReference<ActivityEditLocation2>(target);
    }

    @Override
    public void proceed() {
      ActivityEditLocation2 target = weakTarget.get();
      if (target == null) return;
      ActivityCompat.requestPermissions(target, PERMISSION_STARTDETECTCOORDS, REQUEST_STARTDETECTCOORDS);
    }

    @Override
    public void cancel() {
      ActivityEditLocation2 target = weakTarget.get();
      if (target == null) return;
      target.onLocationDenied();
    }
  }
}
